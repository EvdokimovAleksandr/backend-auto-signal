# 📥 Восстановление базы данных из дампа

## Быстрый старт

### Вариант 1: Автоматическое восстановление (рекомендуется)

**Windows:**
```bash
cd backend-auto-signal
restore-and-sync.bat
```

**Linux/Mac:**
```bash
cd backend-auto-signal
chmod +x restore-and-sync.sh
./restore-and-sync.sh
```

Этот скрипт выполнит:
1. Восстановление БД из дампа
2. Синхронизацию Prisma с реальной структурой БД
3. Проверку данных
4. Тестирование API

### Вариант 2: Пошаговое восстановление

#### Шаг 1: Восстановление дампа

```bash
cd backend-auto-signal
npm run restore-db
```

Этот скрипт:
- Прочитает `DATABASE_URL` из `.env`
- Загрузит дамп из `../new_backup.sql`
- Восстановит все таблицы и данные

#### Шаг 2: Синхронизация Prisma

```bash
npm run sync-prisma
```

Этот скрипт:
- Получит структуру БД через `prisma db pull`
- Обновит `schema.prisma` под реальную структуру
- Сгенерирует Prisma Client

#### Шаг 3: Проверка

```bash
npm run check-db
npm run test-backend
```

## Решение проблем с восстановлением

### Проблема: "psql не найден" или "psql: command not found"

**Решение 1:** Используйте скрипт, который автоматически найдет psql:
```bash
npm run restore-db-simple
```

**Решение 2:** Используйте альтернативный метод через Node.js (требует установки pg):
```bash
npm install pg
npm run restore-db-node
```

**Решение 3:** Найдите psql вручную:
```bash
npm run find-psql
```

Затем используйте полный путь к psql.exe в команде.

### Проблема: "PGPASSWORD не является внутренней командой" (Windows Git Bash)

**Решение:** Используйте PowerShell или CMD вместо Git Bash, или используйте:
```bash
npm run restore-db-simple
```

### Проблема: Неправильное имя базы данных (postgres?schema=public)

**Решение:** Уже исправлено в скрипте. Скрипт автоматически убирает query параметры из имени БД.

## Ручное восстановление

Если автоматические скрипты не работают, можно восстановить вручную:

### Windows (PowerShell)

```powershell
# В PowerShell (не Git Bash!)
# Получите параметры из DATABASE_URL
# Формат: postgresql://user:password@host:port/database

# Пример:
$env:PGPASSWORD="your_password"
psql -h localhost -p 5432 -U postgres -d postgres -f "C:\Users\evd19\OneDrive\Рабочий стол\SIGNAL\new_backup.sql"
```

**Важно:** Используйте PowerShell или CMD, а не Git Bash для этой команды!

### Linux/Mac

```bash
# Получите параметры из DATABASE_URL
export PGPASSWORD="your_password"
psql -h localhost -p 5432 -U your_user -d your_database -f ../new_backup.sql
```

## Проверка после восстановления

### 1. Проверка данных

```bash
npm run check-db
```

Должны увидеть:
- Количество записей в каждой таблице
- Примеры данных
- Итоговую статистику

### 2. Проверка API

```bash
npm run test-backend
```

Должны пройти все тесты:
- ✅ Получение марок
- ✅ Авторизация
- ✅ Получение пользователей
- ✅ И т.д.

### 3. Проверка Prisma

```bash
npx prisma studio
```

Откроется веб-интерфейс для просмотра данных.

## Решение проблем

### Проблема: "psql: command not found"

**Windows:**
- Установите PostgreSQL
- Добавьте `C:\Program Files\PostgreSQL\XX\bin` в PATH
- Или используйте полный путь: `"C:\Program Files\PostgreSQL\XX\bin\psql.exe"`

**Linux:**
```bash
sudo apt-get install postgresql-client
```

**Mac:**
```bash
brew install postgresql
```

### Проблема: "password authentication failed"

**Решение:**
1. Проверьте правильность пароля в `DATABASE_URL`
2. Проверьте настройки `pg_hba.conf` в PostgreSQL
3. Попробуйте подключиться вручную:
   ```bash
   psql -h localhost -U your_user -d your_database
   ```

### Проблема: "could not connect to server"

**Решение:**
1. Убедитесь, что PostgreSQL запущен
2. Проверьте хост и порт в `DATABASE_URL`
3. Проверьте firewall настройки

### Проблема: "relation already exists"

**Решение:**
Если таблицы уже существуют, можно:
1. Удалить их перед восстановлением:
   ```sql
   DROP SCHEMA public CASCADE;
   CREATE SCHEMA public;
   ```
2. Или использовать флаг `--clean` в psql (но это не поддерживается в скрипте)

### Проблема: Prisma schema не синхронизирован

**Решение:**
1. Запустите вручную:
   ```bash
   npx prisma db pull
   npx prisma generate
   ```
2. Проверьте `schema.prisma` на наличие всех таблиц
3. При необходимости исправьте вручную

## Структура дампа

Дамп содержит следующие таблицы:
- `admin_users` - администраторы
- `admins` - старые админы (legacy)
- `bot_settings` - настройки бота
- `brands` - марки автомобилей
- `file_access_stats` - статистика доступа к файлам
- `files` - файлы (фото, PDF)
- `models` - модели автомобилей
- `owner` - владелец бота
- `password` - пароль админа
- `premium_users` - премиум пользователи
- `subscription_prices` - цены подписок
- `users` - пользователи
- `years` - годы выпуска

## Важные замечания

1. **Резервная копия:** Перед восстановлением сделайте резервную копию текущей БД (если есть данные)

2. **DATABASE_URL:** Убедитесь, что `DATABASE_URL` в `.env` указывает на правильную БД

3. **Права доступа:** Убедитесь, что пользователь БД имеет права на создание таблиц и вставку данных

4. **Время выполнения:** Восстановление может занять несколько минут в зависимости от размера дампа

## После восстановления

После успешного восстановления:

1. ✅ Проверьте данные: `npm run check-db`
2. ✅ Протестируйте API: `npm run test-backend`
3. ✅ Запустите backend: `npm run dev`
4. ✅ Проверьте frontend: авторизуйтесь и проверьте работу UI

## Полезные команды

```bash
# Восстановление БД
npm run restore-db

# Синхронизация Prisma
npm run sync-prisma

# Проверка БД
npm run check-db

# Тестирование API
npm run test-backend

# Просмотр данных (веб-интерфейс)
npx prisma studio
```

