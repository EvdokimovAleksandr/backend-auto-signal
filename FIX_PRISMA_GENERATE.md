# 🔧 Решение проблемы EPERM при Prisma generate

## Проблема

При выполнении `npm run sync-prisma` или `npx prisma generate` возникает ошибка:
```
EPERM: operation not permitted, rename '...query_engine-windows.dll.node.tmp...' -> '...query_engine-windows.dll.node'
```

## Причина

Эта ошибка обычно возникает когда:
1. **OneDrive синхронизирует файлы** - файл заблокирован процессом синхронизации
2. **Антивирус сканирует файлы** - файл заблокирован антивирусом
3. **Другой процесс использует файл** - Node.js или другой процесс держит файл открытым

## Решения

### Решение 1: Временно отключить синхронизацию OneDrive (рекомендуется)

1. Закройте все процессы Node.js (остановите `npm run dev`)
2. Щелкните правой кнопкой на иконке OneDrive в трее
3. Выберите "Приостановить синхронизацию" → "2 часа"
4. Выполните:
   ```bash
   npm run sync-prisma
   ```
5. После успешной генерации возобновите синхронизацию OneDrive

### Решение 2: Исключить папку node_modules из синхронизации OneDrive

1. Откройте настройки OneDrive
2. Перейдите в "Учетная запись" → "Выбрать папки"
3. Исключите папку `node_modules` из синхронизации
4. Или переместите проект в папку, которая не синхронизируется с OneDrive

### Решение 3: Закрыть все процессы Node.js

1. Откройте Диспетчер задач (Ctrl+Shift+Esc)
2. Найдите все процессы `node.exe`
3. Завершите их все
4. Выполните:
   ```bash
   npm run sync-prisma
   ```

### Решение 4: Переместить проект из OneDrive

Если проблема повторяется, переместите проект в папку вне OneDrive:
- `C:\Projects\SIGNAL`
- `D:\Projects\SIGNAL`

### Решение 5: Использовать альтернативный метод

Если ничего не помогает, можно запустить команды по отдельности:

```bash
# 1. Получить схему из БД
npx prisma db pull

# 2. Подождать несколько секунд

# 3. Сгенерировать клиент
npx prisma generate
```

## Проверка

После успешной генерации проверьте:

```bash
npm run check-db
npm run test-backend
```

## Предотвращение проблемы в будущем

1. **Исключите `node_modules` из синхронизации OneDrive**
2. **Используйте `.gitignore`** для исключения `node_modules` из Git
3. **Рассмотрите использование WSL2** для разработки на Windows


