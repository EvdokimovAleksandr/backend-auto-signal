# 🚀 Быстрый старт после восстановления БД

## Пошаговая инструкция

### 1. Восстановление базы данных

```bash
cd backend-auto-signal

# Восстановите БД из дампа
npm run restore-db
```

**Что делает:**
- Читает `DATABASE_URL` из `.env`
- Загружает дамп из `new_backup.sql`
- Восстанавливает все таблицы и данные

### 2. Синхронизация Prisma

```bash
# Синхронизируйте Prisma с реальной структурой БД
npm run sync-prisma
```

**Что делает:**
- Получает структуру БД через `prisma db pull`
- Обновляет `schema.prisma`
- Генерирует Prisma Client

### 3. Проверка восстановления

```bash
# Проверьте данные в БД
npm run check-db

# Протестируйте API
npm run test-backend
```

### 4. Запуск сервера

```bash
# Запустите backend
npm run dev
```

Сервер будет доступен на `http://localhost:8000`

### 5. Проверка Frontend

В другом терминале:

```bash
cd frontend-auto-signal
npm run dev
```

Откройте `http://localhost:3000` и попробуйте:
- Авторизоваться (используйте любой user_id из БД)
- Просмотреть марки автомобилей
- Проверить работу UI

## Автоматическое восстановление

Для автоматического выполнения всех шагов:

**Windows:**
```bash
cd backend-auto-signal
restore-and-sync.bat
```

**Linux/Mac:**
```bash
cd backend-auto-signal
chmod +x restore-and-sync.sh
./restore-and-sync.sh
```

## Что делать если что-то не работает

1. **Проверьте DATABASE_URL** в `.env`
2. **Проверьте, что PostgreSQL запущен**
3. **Проверьте логи** в консоли
4. **Запустите диагностику:**
   ```bash
   npm run check-db
   npm run test-backend
   ```

## Полезные команды

```bash
# Восстановление БД
npm run restore-db

# Синхронизация Prisma
npm run sync-prisma

# Проверка БД
npm run check-db

# Тестирование API
npm run test-backend

# Просмотр данных (веб-интерфейс)
npx prisma studio
```


