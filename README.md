# 🚗 Backend Auto Signal

Backend API для проекта Auto Signal на Express.js, TypeScript и PostgreSQL с использованием Prisma ORM.

## 📋 Содержание

- [Описание](#-описание)
- [Требования](#-требования)
- [Быстрый старт](#-быстрый-старт)
- [Установка](#-установка)
  - [1. Установка зависимостей](#1-установка-зависимостей)
  - [2. Настройка базы данных PostgreSQL](#2-настройка-базы-данных-postgresql)
  - [3. Настройка переменных окружения](#3-настройка-переменных-окружения)
  - [4. Восстановление базы данных](#4-восстановление-базы-данных)
  - [5. Синхронизация Prisma](#5-синхронизация-prisma)
- [Запуск проекта](#-запуск-проекта)
- [Тестирование](#-тестирование)
- [Структура проекта](#-структура-проекта)
- [API Документация](#-api-документация)
- [Скрипты и утилиты](#-скрипты-и-утилиты)
- [Устранение неполадок](#-устранение-неполадок)

---

## 📖 Описание

Backend API предоставляет полный функционал для работы с:
- 👥 **Пользователями** - регистрация, авторизация, управление профилями
- 🚗 **Автомобилями** - марки, модели, годы выпуска (CRUD операции)
- 📁 **Файлами** - управление фото и PDF файлами (обычные и премиум)
- 💎 **Подписками** - управление премиум подписками и ценами
- 👑 **Админ-панелью** - статистика, управление администраторами, настройки бота
- 📊 **Статистикой** - детальная статистика использования, топ моделей

---

## 🔧 Требования

Перед началом работы убедитесь, что у вас установлены:

- **Node.js** версии 18.x или выше
- **PostgreSQL** версии 12.x или выше
- **npm** или **yarn**
- **Git** (для клонирования репозитория)

### Проверка версий

```bash
node --version   # Должно быть v18.x или выше
npm --version    # Должно быть 9.x или выше
psql --version   # Должно быть PostgreSQL 12.x или выше
```

---

## 🚀 Быстрый старт

Если у вас уже есть настроенная база данных и файл дампа:

```bash
# 1. Установка зависимостей
npm install

# 2. Настройка .env
cp .env.example .env
# Отредактируйте .env файл, указав DATABASE_URL и JWT_SECRET

# 3. Восстановление БД (если есть дамп)
npm run restore-db-node

# 4. Синхронизация Prisma
npm run sync-prisma

# 5. Проверка БД
npm run check-db

# 6. Запуск сервера
npm run dev
```

Сервер будет доступен по адресу: `http://localhost:8000`

---

## 📦 Установка

### 1. Установка зависимостей

```bash
# Перейдите в директорию проекта
cd backend-auto-signal

# Установите зависимости
npm install
```

### 2. Настройка базы данных PostgreSQL

#### Windows

1. **Установка PostgreSQL:**
   - Скачайте установщик с [официального сайта](https://www.postgresql.org/download/windows/)
   - Запустите установщик и следуйте инструкциям
   - Запомните пароль для пользователя `postgres`

2. **Создание базы данных:**
   ```sql
   -- Откройте pgAdmin или psql
   -- Создайте базу данных
   CREATE DATABASE auto_signal;
   
   -- Создайте пользователя (опционально)
   CREATE USER auto_signal_user WITH PASSWORD 'your_password';
   GRANT ALL PRIVILEGES ON DATABASE auto_signal TO auto_signal_user;
   ```

#### Linux/macOS

1. **Установка PostgreSQL:**
   ```bash
   # Ubuntu/Debian
   sudo apt-get update
   sudo apt-get install postgresql postgresql-contrib
   
   # macOS (через Homebrew)
   brew install postgresql
   brew services start postgresql
   ```

2. **Создание базы данных:**
   ```bash
   # Войдите в PostgreSQL
   sudo -u postgres psql
   
   # Создайте базу данных
   CREATE DATABASE auto_signal;
   \q
   ```

#### Проверка подключения

```bash
# Проверьте, что PostgreSQL запущен
# Windows: через службы Windows
# Linux: sudo systemctl status postgresql
# macOS: brew services list

# Проверьте подключение
psql -U postgres -d auto_signal -c "SELECT version();"
```

### 3. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```bash
# Скопируйте пример файла
cp .env.example .env
```

Откройте `.env` и настройте следующие переменные:

```env
# База данных PostgreSQL
DATABASE_URL="postgresql://username:password@localhost:5432/auto_signal?schema=public"

# JWT секретный ключ (обязательно!)
# Сгенерируйте случайную строку, например: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-this-in-production"

# Порт сервера (по умолчанию 8000)
PORT=8000

# Telegram Bot Token (опционально)
# Нужен для автоматического получения User ID по username
# Получите токен через @BotFather в Telegram
TELEGRAM_BOT_TOKEN="your_telegram_bot_token_here"

# URL API (для тестов)
API_URL="http://localhost:8000"
```

**⚠️ Важно:**
- `DATABASE_URL` - строка подключения к PostgreSQL в формате: `postgresql://user:password@host:port/database`
- `JWT_SECRET` - обязательно измените на случайную строку для безопасности
- `TELEGRAM_BOT_TOKEN` - опционально, но рекомендуется для полного функционала

### 4. Восстановление базы данных

Если у вас есть файл дампа базы данных (`backup.sql` или `new_backup.sql`):

#### Вариант 1: Через Node.js скрипт (рекомендуется)

```bash
# Убедитесь, что файл дампа находится в корне проекта или на уровень выше
# Скрипт автоматически найдет backup.sql или new_backup.sql

npm run restore-db-node
```

#### Вариант 2: Через PostgreSQL напрямую

```bash
# Windows (PowerShell)
psql -U postgres -d auto_signal -f backup.sql

# Linux/macOS
psql -U postgres -d auto_signal < backup.sql
```

#### Вариант 3: Автоматический скрипт (Windows/Linux)

```bash
# Windows
restore-and-sync.bat

# Linux/macOS
chmod +x restore-and-sync.sh
./restore-and-sync.sh
```

**После восстановления БД обязательно выполните синхронизацию Prisma!**

### 5. Синхронизация Prisma

После восстановления базы данных необходимо синхронизировать Prisma схему:

```bash
# Синхронизация Prisma с существующей БД
npm run sync-prisma

# Или вручную:
npx prisma db pull    # Получить схему из БД
npx prisma generate   # Сгенерировать Prisma Client
```

**Что делает скрипт:**
1. Получает схему базы данных (`prisma db pull`)
2. Генерирует Prisma Client для работы с БД (`prisma generate`)

---

## 🏃 Запуск проекта

### Режим разработки (с автоперезагрузкой)

```bash
npm run dev
```

Сервер запустится на `http://localhost:8000` (или порт, указанный в `PORT`)

### Production режим

```bash
# Сборка TypeScript
npm run build

# Запуск скомпилированного проекта
npm start
```

### Проверка работы сервера

Откройте в браузере:
- **API документация (Swagger):** `http://localhost:8000/api-docs`
- **Список маршрутов:** `http://localhost:8000/debug/routes`
- **Проверка БД:** `npm run check-db`

---

## 🧪 Тестирование

### Запуск unit-тестов (Jest)

```bash
# Запуск всех тестов
npm test

# Запуск с покрытием
npm test -- --coverage
```

**Тесты проверяют:**
- ✅ Авторизацию и регистрацию пользователей
- ✅ Валидацию входных данных
- ✅ Работу JWT токенов
- ✅ Интеграционные сценарии

### Тестирование API (требует запущенный сервер)

```bash
# Убедитесь, что сервер запущен (npm run dev)
# В другом терминале:
npm run test-backend
```

**Скрипт проверяет:**
- ✅ Доступность базовых эндпоинтов
- ✅ Работу авторизации
- ✅ Получение токена
- ✅ Проверку `/api/users/me`

### Проверка базы данных

```bash
# Детальная проверка всех таблиц
npm run check-db

# Проверка подключения Prisma
node check-prisma.js
```

---

## 📁 Структура проекта

```
backend-auto-signal/
├── src/
│   ├── config/          # Конфигурация (Swagger)
│   ├── controllers/     # Контроллеры API
│   │   ├── adminController.js
│   │   ├── carsController.js
│   │   ├── filesController.js
│   │   ├── infoController.js
│   │   ├── subscriptionController.js
│   │   └── usersController.js
│   ├── middleware/      # Middleware (auth, admin)
│   │   ├── accessCheck.js
│   │   ├── adminCheck.js
│   │   └── auth.js
│   ├── routes/          # Маршруты Express
│   │   ├── admin.js
│   │   ├── cars.js
│   │   ├── files.js
│   │   ├── info.js
│   │   ├── subscription.js
│   │   └── users.js
│   ├── tests/           # Тесты
│   │   ├── app.js
│   │   ├── auth.test.js
│   │   └── setup.js
│   ├── utils/           # Утилиты
│   │   ├── database.js      # Prisma Client
│   │   ├── fileUtils.js
│   │   ├── googleDrive.js
│   │   ├── pagination.js
│   │   ├── telegramBot.js
│   │   └── validation.js
│   └── index.ts         # Точка входа приложения
├── prisma/
│   ├── migrations/     # Миграции базы данных
│   └── schema.prisma    # Схема Prisma
├── check-database.js    # Скрипт проверки БД
├── check-prisma.js      # Скрипт проверки Prisma
├── restore-database-node.js  # Восстановление БД
├── restore-admins.js    # Восстановление админов
├── sync-prisma.js       # Синхронизация Prisma
├── test-backend.js      # Тестирование API
├── jest.config.js       # Конфигурация Jest
├── tsconfig.json        # Конфигурация TypeScript
├── package.json         # Зависимости и скрипты
└── README.md           # Этот файл
```

---

## 📚 API Документация

### Swagger UI

После запуска сервера откройте в браузере:
```
http://localhost:8000/api-docs
```

Здесь вы найдете полную интерактивную документацию всех эндпоинтов.

### Основные эндпоинты

#### 🔐 Авторизация
- `POST /api/users/login` - Вход/регистрация пользователя
- `POST /api/users/register` - Регистрация нового пользователя
- `GET /api/users/me` - Получить текущего пользователя (требует токен)

#### 🚗 Автомобили
- `GET /api/cars/brands` - Получить все марки
- `GET /api/cars/brands/search?name=X` - Поиск марок
- `POST /api/cars/brands` - Добавить марку (админ)
- `PUT /api/cars/brands` - Обновить марку (админ)
- `DELETE /api/cars/brands/:id` - Удалить марку (админ)
- `GET /api/cars/models?brandId=X` - Получить модели по марке
- `GET /api/cars/models/search?name=X` - Поиск моделей
- `GET /api/cars/years?modelId=X` - Получить годы по модели
- `GET /api/cars/years/search?year=X` - Поиск годов

#### 📁 Файлы
- `GET /api/files/years/:yearId/files?userId=X` - Получить файлы по году
- `POST /api/files/photos` - Добавить фото (админ)
- `DELETE /api/files/photos/:fileId` - Удалить фото (админ)
- `POST /api/files/pdfs` - Добавить PDF (админ)
- `GET /api/files/image/:fileId` - Прокси для изображений

#### 💎 Подписки
- `GET /api/subscription/prices` - Получить цены подписок
- `GET /api/subscription/user/:userId` - Получить подписку пользователя
- `POST /api/subscription/user` - Создать/обновить подписку
- `DELETE /api/subscription/user/:userId` - Удалить подписку

#### 👑 Админ-панель
- `GET /api/admin/stats` - Базовая статистика (админ)
- `GET /api/admin/stats/detailed` - Детальная статистика (админ)
- `GET /api/admin/stats/top-models` - Топ-20 моделей (админ)
- `GET /api/admin/admins/list` - Список администраторов (админ)
- `POST /api/admin/admins` - Добавить администратора (админ)
- `DELETE /api/admin/admins/:userId` - Удалить администратора (админ)
- `GET /api/admin/prices` - Управление ценами (админ)
- `PUT /api/admin/prices/:periodMonths` - Обновить цену (админ)
- `GET /api/admin/settings` - Настройки бота (админ)
- `PUT /api/admin/settings/start-message` - Обновить стартовое сообщение (админ)

#### ℹ️ Информация
- `GET /api/info/help` - Получить справку

### Аутентификация

Большинство админ-эндпоинтов требуют JWT токен в заголовке:

```
Authorization: Bearer <your_jwt_token>
```

Получить токен можно через `POST /api/users/login`.

---

## 🛠️ Скрипты и утилиты

### NPM скрипты

```bash
# Разработка
npm run dev              # Запуск в режиме разработки (nodemon + ts-node)

# Production
npm run build            # Сборка TypeScript в JavaScript
npm start                # Запуск скомпилированного проекта

# Тестирование
npm test                 # Запуск Jest тестов
npm run test-backend     # Тестирование API (требует запущенный сервер)

# База данных
npm run check-db         # Проверка базы данных
npm run restore-db-node # Восстановление БД из дампа
npm run sync-prisma      # Синхронизация Prisma с БД
```

### Утилитарные скрипты

```bash
# Проверка подключения к БД
node check-database.js

# Проверка Prisma
node check-prisma.js

# Восстановление админов (если нужно)
node restore-admins.js

# Тестирование API
node test-backend.js
```

### Автоматические скрипты

```bash
# Windows
restore-and-sync.bat     # Восстановление БД + синхронизация Prisma

# Linux/macOS
chmod +x restore-and-sync.sh
./restore-and-sync.sh    # Восстановление БД + синхронизация Prisma
```

---

## 🔍 Устранение неполадок

### Проблема: Не удается подключиться к базе данных

**Решение:**
1. Проверьте, что PostgreSQL запущен:
   ```bash
   # Windows: через службы Windows
   # Linux: sudo systemctl status postgresql
   # macOS: brew services list
   ```

2. Проверьте `DATABASE_URL` в `.env`:
   ```env
   DATABASE_URL="postgresql://username:password@localhost:5432/auto_signal"
   ```

3. Проверьте подключение:
   ```bash
   psql -U postgres -d auto_signal -c "SELECT 1;"
   ```

4. Запустите проверку:
   ```bash
   npm run check-db
   ```

### Проблема: Prisma не может подключиться

**Решение:**
1. Убедитесь, что Prisma Client сгенерирован:
   ```bash
   npx prisma generate
   ```

2. Синхронизируйте схему:
   ```bash
   npm run sync-prisma
   ```

3. Проверьте схему:
   ```bash
   node check-prisma.js
   ```

### Проблема: Ошибка "JWT_SECRET is not defined"

**Решение:**
1. Убедитесь, что файл `.env` существует
2. Проверьте, что `JWT_SECRET` указан в `.env`
3. Перезапустите сервер

### Проблема: Тесты не проходят

**Решение:**
1. Убедитесь, что база данных настроена и доступна
2. Проверьте переменные окружения в `.env`
3. Запустите проверку БД:
   ```bash
   npm run check-db
   ```

### Проблема: Ошибка при восстановлении БД

**Решение:**
1. Убедитесь, что файл дампа существует (`backup.sql` или `new_backup.sql`)
2. Проверьте права доступа к файлу
3. Убедитесь, что база данных создана и пуста
4. Попробуйте восстановить через psql напрямую:
   ```bash
   psql -U postgres -d auto_signal < backup.sql
   ```

### Проблема: Порт 8000 уже занят

**Решение:**
1. Измените порт в `.env`:
   ```env
   PORT=8001
   ```

2. Или найдите и остановите процесс, использующий порт:
   ```bash
   # Windows
   netstat -ano | findstr :8000
   taskkill /PID <PID> /F
   
   # Linux/macOS
   lsof -ti:8000 | xargs kill
   ```

---

## 📝 Дополнительная информация

### Переменные окружения

| Переменная | Описание | Обязательно | По умолчанию |
|------------|----------|-------------|--------------|
| `DATABASE_URL` | Строка подключения к PostgreSQL | ✅ Да | - |
| `JWT_SECRET` | Секретный ключ для JWT токенов | ✅ Да | `default-secret-key` |
| `PORT` | Порт сервера | ❌ Нет | `8000` |
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | ❌ Нет | - |
| `API_URL` | URL API для тестов | ❌ Нет | `http://localhost:8000` |

### Технологии

- **Express.js** 5.x - веб-фреймворк
- **TypeScript** 5.x - типизированный JavaScript
- **PostgreSQL** - реляционная база данных
- **Prisma** 6.x - современный ORM
- **JWT** - аутентификация
- **Jest** - тестирование
- **Swagger** - документация API

### Безопасность

- ✅ JWT токены для аутентификации
- ✅ Middleware для проверки прав администратора
- ✅ Валидация входных данных
- ✅ CORS настроен для безопасности
- ⚠️ **Важно:** Измените `JWT_SECRET` в production!

---

## 📞 Поддержка

Если возникли проблемы:

1. Проверьте раздел [Устранение неполадок](#-устранение-неполадок)
2. Запустите проверки:
   ```bash
   npm run check-db
   node check-prisma.js
   npm run test-backend
   ```
3. Проверьте логи сервера в консоли

---

## 📄 Лицензия

ISC

---

**🎉 Готово! Теперь вы можете запустить backend на любом компьютере!**
