# 🚗 Backend Auto Signal

Backend API для проекта Auto Signal, работающий с базой данных PostgreSQL через Prisma ORM.

---

## 📋 Содержание

- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Запуск проекта](#-запуск-проекта)
- [API Endpoints](#-api-endpoints)
- [Интеграция с фронтендом](#-интеграция-с-фронтендом)
- [Тестирование API](#-тестирование-api)
- [Структура проекта](#-структура-проекта)
- [Настройка Prisma](#-настройка-prisma)
- [Решение проблем](#-решение-проблем)
- [Технологии](#-технологии)

---

## 🚀 Быстрый старт

```bash
# 1. Установите зависимости
npm install

# 2. Создайте .env файл (см. .env.example)
cp .env.example .env

# 3. Настройте DATABASE_URL в .env

# 4. Синхронизируйте схему с БД
npx prisma db push

# 5. Сгенерируйте Prisma Client
npx prisma generate

# 6. Запустите сервер
npm run dev
```

Сервер будет доступен на `http://localhost:8000`

---

## ⚙️ Установка и настройка

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Настройте следующие переменные:

```env
# База данных
DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"

# Сервер
PORT=8000
NODE_ENV=development

# JWT для аутентификации
JWT_SECRET=your-secret-key-change-this-in-production
```

### 3. Настройка базы данных

#### Если база данных уже существует:

```bash
# Синхронизируйте схему с существующей БД
npx prisma db push

# Сгенерируйте Prisma Client
npx prisma generate
```

#### Если база данных пустая:

```bash
# Примените миграции
npx prisma migrate deploy

# Или создайте новую миграцию
npx prisma migrate dev --name init

# Сгенерируйте Prisma Client
npx prisma generate
```

### 4. Проверка подключения

```bash
npm run check-prisma
```

Должны пройти все проверки ✅

---

## 🏃 Запуск проекта

### Режим разработки

```bash
npm run dev
```

Сервер запустится на порту 8000 (или указанном в `.env`)

### Production

```bash
# Соберите проект
npm run build

# Запустите
npm start
```

### Доступные команды

```bash
npm run dev          # Запуск в режиме разработки
npm run build        # Сборка проекта
npm start            # Запуск production версии
npm run test         # Запуск тестов
npm run test-api     # Тестирование API эндпоинтов
npm run check-prisma # Проверка подключения Prisma
```

---

## 📡 API Endpoints

### 🚗 Автомобили

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/cars/brands` | Получить все марки | - |
| POST | `/api/cars/brands` | Добавить марку(и) | Админ |
| DELETE | `/api/cars/brands/:id` | Удалить марку | Админ |
| GET | `/api/cars/models?brandId=X` | Получить модели по марке | - |
| POST | `/api/cars/models` | Добавить модель | Админ |
| GET | `/api/cars/years/search?year=X&modelId=Y` | Поиск годов | - |

### 📁 Файлы

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/files/years/:yearId/files` | Получить файлы по году | - |
| POST | `/api/files/photos` | Добавить фото | Админ |
| POST | `/api/files/pdfs` | Добавить PDF | Админ |
| POST | `/api/files/photos/premium` | Добавить премиум фото | Админ |
| POST | `/api/files/pdfs/premium` | Добавить премиум PDF | Админ |

### 👤 Пользователи

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| POST | `/api/users/register` | Регистрация пользователя | - |
| GET | `/api/users/:userId` | Получить информацию о пользователе | - |
| PUT | `/api/users/:userId` | Обновить информацию о пользователе | - |
| GET | `/api/users` | Получить список пользователей (с пагинацией) | - |

### 💳 Подписки

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/subscription/prices` | Получить цены подписок | - |
| GET | `/api/subscription/user/:userId` | Получить подписку пользователя | - |
| POST | `/api/subscription/user` | Создать/обновить подписку | - |
| DELETE | `/api/subscription/user/:userId` | Удалить подписку | - |

### 🔐 Админ панель

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/admin/stats` | Статистика | Админ |
| GET | `/api/admin/admins` | Список администраторов | Админ |
| POST | `/api/admin/admins` | Добавить администратора | Админ |
| DELETE | `/api/admin/admins/:userId` | Удалить администратора | Админ |
| GET | `/api/admin/prices` | Управление ценами | Админ |
| PUT | `/api/admin/prices/:periodMonths` | Обновить цену | Админ |

### ℹ️ Информация

| Метод | Endpoint | Описание | Аутентификация |
|-------|----------|----------|----------------|
| GET | `/api/info/help` | Получить справку | - |
| GET | `/debug/routes` | Список всех маршрутов | - |

---

## 🌐 Интеграция с фронтендом

### ✅ Подтверждение работоспособности

**Все изменения в БД сохраняются автоматически!**

Prisma автоматически коммитит все операции (`create`, `update`, `delete`, `upsert`) в базу данных. Когда вы вызываете эндпоинт, который изменяет данные, изменения **сразу сохраняются** в PostgreSQL.

### 🔄 Как это работает

1. **Вы отправляете запрос** с фронтенда → бэкенд получает его
2. **Контроллер выполняет операцию** → `prisma.users.create()`, `prisma.brands.update()` и т.д.
3. **Prisma автоматически коммитит** → изменения сохраняются в БД
4. **Ответ возвращается** → вы получаете обновленные данные

### 📝 Примеры использования

#### Регистрация пользователя

```javascript
const response = await fetch('http://localhost:8000/api/users/register', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    userId: '123456789',
    username: 'test_user',
    name: 'Test User'
  })
});

const user = await response.json();
// Пользователь создан и сохранен в БД!
```

#### Получение марок автомобилей

```javascript
const response = await fetch('http://localhost:8000/api/cars/brands');
const brands = await response.json();
// Получаете список всех марок
```

#### Обновление пользователя

```javascript
const response = await fetch(`http://localhost:8000/api/users/${userId}`, {
  method: 'PUT',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({
    name: 'New Name',
    stage: 5
  })
});

const updatedUser = await response.json();
// Данные обновлены в БД!
```

### ⚠️ Важные моменты

#### 1. BigInt значения

Все `user_id` (BigInt) автоматически преобразуются в строки в JSON ответах:

```javascript
// В ответе user_id будет строкой
{
  "id": 1,
  "user_id": "123456789",  // строка, не число!
  "username": "test"
}
```

#### 2. Админ эндпоинты

Некоторые эндпоинты требуют JWT токен в заголовке:

```javascript
headers: {
  'Content-Type': 'application/json',
  'Authorization': 'Bearer YOUR_JWT_TOKEN'
}
```

#### 3. Обработка ошибок

Всегда проверяйте статус ответа:

```javascript
const response = await fetch(url, options);
if (!response.ok) {
  const error = await response.json();
  console.error('Ошибка:', error.error);
  return;
}
const data = await response.json();
```

---

## 📖 Swagger документация

### Доступ к Swagger UI

После запуска сервера откройте в браузере:

```
http://localhost:8000/api-docs
```

Swagger UI предоставляет:
- 📋 Полный список всех API эндпоинтов
- 📝 Описание параметров и ответов
- 🧪 Интерактивное тестирование API прямо в браузере
- 📚 Документацию всех моделей данных
- 🔐 Информацию о требованиях аутентификации

### Использование Swagger UI

1. Откройте `http://localhost:8000/api-docs` в браузере
2. Выберите нужный эндпоинт
3. Нажмите "Try it out"
4. Заполните параметры (если требуется)
5. Нажмите "Execute"
6. Посмотрите ответ сервера

### Аутентификация в Swagger

Для тестирования админ эндпоинтов:
1. Нажмите кнопку "Authorize" в правом верхнем углу
2. Введите JWT токен в формате: `Bearer YOUR_TOKEN`
3. Нажмите "Authorize"
4. Теперь можно тестировать защищенные эндпоинты

---

## 🧪 Тестирование API

### Способ 1: Swagger UI (рекомендуется)

Откройте `http://localhost:8000/api-docs` для интерактивного тестирования.

### Способ 2: Встроенный debug endpoint

Откройте в браузере:
```
http://localhost:8000/debug/routes
```

Этот endpoint покажет все доступные маршруты и методы.

### Способ 2: Автоматическое тестирование

```bash
npm run test-api
```

Скрипт автоматически протестирует основные эндпоинты.

### Способ 3: Использование curl

```bash
# Получить все марки
curl http://localhost:8000/api/cars/brands

# Зарегистрировать пользователя
curl -X POST http://localhost:8000/api/users/register \
  -H "Content-Type: application/json" \
  -d '{"userId": "123456789", "username": "test", "name": "Test User"}'

# Получить информацию о пользователе
curl http://localhost:8000/api/users/123456789
```

### Способ 4: Использование браузера

Для GET запросов можно просто открыть в браузере:
- `http://localhost:8000/api/cars/brands`
- `http://localhost:8000/api/info/help`
- `http://localhost:8000/debug/routes`

### Способ 5: Postman

1. Установите Postman
2. Создайте новый запрос
3. Укажите метод (GET/POST) и URL
4. Для POST добавьте Body → raw → JSON

---

## 📁 Структура проекта

```
backend-auto-signal/
├── src/
│   ├── controllers/    # Контроллеры для обработки запросов
│   │   ├── adminController.js
│   │   ├── carsController.js
│   │   ├── filesController.js
│   │   ├── infoController.js
│   │   ├── subscriptionController.js
│   │   └── usersController.js
│   ├── middleware/     # Middleware (аутентификация, проверка прав)
│   │   ├── adminCheck.js
│   │   ├── auth.js
│   │   └── accessCheck.js
│   ├── routes/         # Маршруты API
│   │   ├── admin.js
│   │   ├── cars.js
│   │   ├── files.js
│   │   ├── info.js
│   │   ├── subscription.js
│   │   └── users.js
│   ├── utils/          # Утилиты
│   │   ├── database.js      # Prisma клиент
│   │   ├── fileUtils.js
│   │   ├── googleDrive.js
│   │   ├── pagination.js
│   │   └── validation.js
│   └── index.ts        # Точка входа приложения
├── prisma/
│   ├── schema.prisma  # Схема базы данных
│   └── migrations/    # Миграции БД
├── .env.example       # Пример файла окружения
├── package.json
├── tsconfig.json
└── README.md          # Этот файл
```

---

## 🔧 Настройка Prisma

### Проблема: Prisma Client не сгенерирован

Если вы видите ошибки типа:
```
Invalid `prisma.brands.findMany()` invocation
```

### Решение:

1. **Остановите сервер** (если запущен)

2. **Сгенерируйте Prisma Client:**
   ```bash
   npx prisma generate
   ```

3. **Если ошибка EPERM:**
   - Убедитесь, что сервер остановлен
   - Закройте все IDE/редакторы
   - Попробуйте снова

4. **Проверьте подключение:**
   ```bash
   npm run check-prisma
   ```

### Синхронизация схемы с БД

```bash
# Применить схему к существующей БД
npx prisma db push

# Или создать миграцию
npx prisma migrate dev --name sync_schema
```

---

## 🐛 Решение проблем

### Проблема: "The column users.id does not exist"

**Решение:**

1. Остановите сервер
2. Выполните:
   ```bash
   npx prisma db push
   npx prisma generate
   ```
3. Проверьте:
   ```bash
   npm run check-prisma
   ```

### Проблема: "Do not know how to serialize a BigInt"

**Решение:** Уже исправлено! BigInt значения автоматически преобразуются в строки через middleware.

### Проблема: EPERM при prisma generate

**Решение:**

1. Остановите сервер
2. Закройте все процессы, использующие Prisma
3. Попробуйте снова:
   ```bash
   npx prisma generate
   ```

Если не помогает:
```bash
rm -rf node_modules/.prisma
npx prisma generate
```

### Проблема: Ошибка подключения к БД

**Решение:**

1. Проверьте, что PostgreSQL запущен
2. Проверьте `DATABASE_URL` в `.env`
3. Проверьте права доступа к БД
4. Запустите:
   ```bash
   npm run check-prisma
   ```

---

## 🛠 Технологии

- **Node.js** - среда выполнения
- **Express.js** - веб-фреймворк
- **TypeScript** - типизированный JavaScript
- **Prisma ORM** - ORM для работы с БД
- **PostgreSQL** - база данных
- **JWT** - аутентификация
- **CORS** - поддержка кросс-доменных запросов

---

## 📚 Дополнительная информация

### Формат данных

- **Даты:** ISO строки для подписок
- **BigInt:** `user_id` преобразуются в строки в JSON
- **Пагинация:** Используйте `?page=1&limit=10` для списков

### Безопасность

- Админ эндпоинты требуют JWT токен
- CORS настроен для кросс-доменных запросов
- Валидация данных на уровне контроллеров

### Производительность

- Prisma автоматически оптимизирует запросы
- Индексы настроены для часто используемых полей
- Пагинация для больших списков

---

## ✅ Готово к использованию!

Бэкенд полностью готов для интеграции с фронтендом и телеграм-ботом. Все операции с БД работают корректно и изменения сохраняются автоматически.

**Удачной разработки! 🚀**
