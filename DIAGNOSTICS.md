# 🔍 Диагностика проблем

## Восстановление БД из дампа

Если у вас есть дамп БД (new_backup.sql), сначала восстановите его:

```bash
npm run restore-db
npm run sync-prisma
```

📖 **Подробная инструкция:** [RESTORE_DATABASE.md](./RESTORE_DATABASE.md)

## Быстрая проверка

### 1. Проверка базы данных

```bash
cd backend-auto-signal
npm run check-db
```

Этот скрипт покажет:
- Подключение к БД
- Количество записей в каждой таблице
- Примеры данных
- Итоговую статистику

### 2. Проверка Backend API

```bash
cd backend-auto-signal
npm run test-backend
```

Этот скрипт проверит:
- Работу базовых эндпоинтов
- Авторизацию
- Получение данных

### 3. Добавление тестовых данных

Если БД пуста, добавьте тестовые данные:

```bash
cd backend-auto-signal
npm run add-test-data
```

Это добавит:
- Тестового пользователя (user_id: 123456789)
- Тестового админа
- Несколько марок автомобилей
- Несколько моделей
- Цены подписок

## Пошаговая диагностика

### Шаг 1: Проверка подключения к БД

1. Убедитесь, что PostgreSQL запущен
2. Проверьте `.env` файл:
   ```env
   DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"
   ```
3. Запустите:
   ```bash
   npm run check-prisma
   ```

### Шаг 2: Проверка структуры БД

```bash
npm run check-db
```

Если таблицы пусты, добавьте тестовые данные:
```bash
npm run add-test-data
```

### Шаг 3: Проверка Backend

1. Убедитесь, что сервер запущен:
   ```bash
   npm run dev
   ```

2. Проверьте, что сервер отвечает:
   ```bash
   curl http://localhost:8000/api/info/help
   ```

3. Запустите тесты:
   ```bash
   npm run test-backend
   ```

### Шаг 4: Проверка Frontend

1. Убедитесь, что frontend запущен:
   ```bash
   cd frontend-auto-signal
   npm run dev
   ```

2. Откройте консоль браузера (F12) и проверьте:
   - Нет ли ошибок в консоли
   - Работают ли запросы к API
   - Правильно ли настроен API_BASE_URL

3. Проверьте `.env` файл frontend:
   ```env
   VITE_API_BASE_URL=http://localhost:8000/api
   ```

## Частые проблемы

### Проблема: БД пуста

**Решение:**
```bash
npm run add-test-data
```

### Проблема: Не могу авторизоваться

**Проверьте:**
1. Запущен ли backend сервер
2. Правильно ли работает эндпоинт `/api/users/login`
3. Проверьте консоль браузера на ошибки

**Тест:**
```bash
npm run test-backend
```

### Проблема: Frontend не подключается к Backend

**Проверьте:**
1. Backend запущен на `http://localhost:8000`
2. В `.env` frontend указан правильный `VITE_API_BASE_URL`
3. CORS настроен правильно в backend

### Проблема: Ошибки в консоли браузера

1. Откройте DevTools (F12)
2. Перейдите на вкладку Console
3. Проверьте ошибки
4. Перейдите на вкладку Network
5. Проверьте, какие запросы не проходят

## Тестовые данные

После запуска `npm run add-test-data`:

- **User ID для входа:** `123456789`
- **Этот пользователь является админом**
- **Марки:** Toyota, BMW, Mercedes-Benz, Audi, Volkswagen
- **Модели Toyota:** Camry, Corolla, RAV4, Prius
- **Цены подписок:** 1, 3, 6, 12 месяцев

## Полезные команды

```bash
# Проверка подключения Prisma
npm run check-prisma

# Детальная проверка БД
npm run check-db

# Тестирование Backend API
npm run test-backend

# Добавление тестовых данных
npm run add-test-data

# Запуск backend
npm run dev

# Запуск frontend (из папки frontend-auto-signal)
npm run dev
```

